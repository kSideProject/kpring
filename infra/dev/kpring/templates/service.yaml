apiVersion: v1
kind: Service
metadata:
  name: {{ include "kpring.fullname" . }}
  labels:
    {{- include "kpring.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kpring.selectorLabels" . | nindent 4 }}
