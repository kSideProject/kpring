apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kpring.fullname" . }}-test-connection"
  labels:
    {{- include "kpring.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kpring.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
